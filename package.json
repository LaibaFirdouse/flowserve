{
    "scripts": {
        "frontend": "cd frontend && npm run dev",
        "backend": "cd backend && node server.js",
        "migrate": "cd backend && npm run migrate"
    },
    "dependencies": {
        "dotenv": "^17.3.1"
    }
}